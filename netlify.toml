[build]
  base = "frontend"
  command = "corepack disable && npm install --legacy-peer-deps --no-audit --no-fund && npm run build"
  publish = "frontend/build"

[build.environment]
  NODE_VERSION = "20"
  NETLIFY_USE_YARN = "false"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
